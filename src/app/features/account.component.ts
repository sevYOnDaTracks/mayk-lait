import { Component, DestroyRef, inject, signal } from '@angular/core';
import { NavigationEnd, Router, RouterLink, RouterOutlet } from '@angular/router';
import { filter } from 'rxjs';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import { AuthService } from '../core/auth.service';
@Component({selector:'app-account',imports:[RouterLink,RouterOutlet],template:`<section class="account page-container">@if(overview()){<div class="account-profile-head"><div class="account-avatar">@if(auth.user()?.photoURL){<img [src]="auth.user()?.photoURL" alt="Photo de profil">}@else{<span>{{initials()}}</span>}</div><div class="account-head"><p class="eyebrow">TON ESPACE</p><h1>Bonjour {{auth.user()?.firstName}} 👋</h1><p>Tout ce qu’il te faut, au même endroit.</p></div></div><nav class="account-cards"><a routerLink="commandes"><span>01</span><div><h2>Mes commandes</h2><p>Suivre et retrouver tes commandes</p></div><b>→</b></a><a routerLink="adresses"><span>02</span><div><h2>Mes adresses</h2><p>Gérer tes lieux de livraison</p></div><b>→</b></a><a routerLink="profil"><span>03</span><div><h2>Mes informations</h2><p>Coordonnées et préférences</p></div><b>→</b></a></nav><button class="text-button logout" (click)="logout()">Se déconnecter</button>}@else{<div class="account-subnav"><a routerLink="/compte">← Mon compte</a><button class="text-button" (click)="logout()">Se déconnecter</button></div>}<router-outlet/></section>`})
export class AccountComponent{readonly auth=inject(AuthService);private router=inject(Router);readonly overview=signal(this.router.url==='/compte');constructor(){this.router.events.pipe(filter(event=>event instanceof NavigationEnd),takeUntilDestroyed(inject(DestroyRef))).subscribe(()=>this.overview.set(this.router.url==='/compte'))}initials(){const user=this.auth.user();return`${user?.firstName?.[0]??''}${user?.lastName?.[0]??''}`.toUpperCase()||'MK'}logout(){this.auth.signOut();this.router.navigateByUrl('/')}}
